services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: bookiops
      POSTGRES_PASSWORD: bookiops
      POSTGRES_DB: bookiops
    ports:
      - '5433:5432'
    volumes:
      - bookiops_pg:/var/lib/postgresql/data

volumes:
  bookiops_pg:
